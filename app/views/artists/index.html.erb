<!-- will create search form -->
<div class="container-fluid">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <h1>Artists</h1>
      <%= link_to 'New Artist', new_artist_path, class: "btn btn-success btn-block", style: "color:white;" %>
    </div>
  </div>

  <hr>

  <div class="row no-gutter">
    <% @artists.each do |artist| %>
    <div class="col-lg-4 col-sm-6">
      <a href="#" class="portfolio-box">
        <%= image_tag artist.artist_image, class: "img-responsive", style: "height:250px;", alt: artist.artist %>
        <div class="portfolio-box-caption">
          <div class="portfolio-box-caption-content">
            <div class="project-category text-faded">
              <%= artist.artist %>
            </div>
          </div>
        </div>
      </a>
    </div>
    <% end %>
  </div>
</div>

<!-- <div class="row">
    <table>
      <thead>
        <tr>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @artists.each do |artist| %>
        <tr>
          <td><%= image_tag(artist.artist_image, width: "400px;", height: "300px") %></td>
        </tr>
        <tr>
          <td><%= artist.artist %></td>
        </tr>
        <tr>
          <td><%= link_to 'Show', artist %></td>
          <td><%= link_to 'Edit', edit_artist_path(artist) %></td>
          <td><%= link_to 'Destroy', artist, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
</div> -->
