.col-lg-3.col-sm-6.artist
  = link_to artist_path(artist.id), class: "portfolio-box", ontouchstart: "" do
    .image
      = image_tag artist.images.empty? ? Constants::DEFAULT_IMG_URL : artist.images.first["url"], class: "img-responsive img-rounded", alt: artist.name
    .portfolio-box-caption.img-rounded
      .portfolio-box-caption-content
        .project-category.text-faded
          h4
            = artist.name
